import React from 'react'
import { withSiteData } from 'react-static'

export default withSiteData(() => (
  <div className="page-content profile-page">
    <h1 className="intro">Process</h1>
  </div>
))
